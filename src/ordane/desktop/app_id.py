"""The application id, in one place.

`app.py` sets the default icon from it and `menu.py` shows it in the about
dialog, and importing one from the other would be a cycle.
"""

APP_ID = "com.kingletas.Ordane"
